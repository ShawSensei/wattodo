import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('placeholder', (WidgetTester tester) async {
    // Integration tests require Hive init — unit tests are preferred.
    expect(true, isTrue);
  });
}
